# Optional. Copy to /srv/organs/io_interface/io_interface.env if needed.

# IO_BASE_URL=http://localhost:8009
# ORGAN_REGISTRY_URL=http://localhost:8000

# No data directory — this organ is stateless, same as Critic. The
# catalog lives in io_interface_core.py itself. See it with:
#   curl localhost:8009/io/catalog
