# Optional. Copy to /srv/organs/executive/executive.env if needed.

# EXECUTIVE_DATA_DIR=/srv/organs/executive/data
# EXECUTIVE_BASE_URL=http://localhost:8008
# ORGAN_REGISTRY_URL=http://localhost:8000

# Executive REQUIRES Critic to be reachable to submit a plan safely.
# If Critic is down, every step fails closed to high_risk/requires
# approval — plans still get created, they just all need a human to
# clear every step, which is the safe direction to fail in.
