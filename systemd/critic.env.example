# Optional. Copy to /srv/organs/critic/critic.env if needed.

# CRITIC_BASE_URL=http://localhost:8007
# ORGAN_REGISTRY_URL=http://localhost:8000

# Critic is stateless and deterministic — no model, no data dir. The
# rule set lives in critic_core.py itself. To see it: GET /critic/rules
